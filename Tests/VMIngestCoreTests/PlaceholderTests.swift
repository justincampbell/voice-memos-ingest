import XCTest
@testable import VMIngestCore

// Placeholder so the test target compiles from step 1. Real tests for epoch
// conversion, tsrp extraction, and producer-state diffing arrive in later steps.
final class PlaceholderTests: XCTestCase {
    func testPackageBuilds() {
        XCTAssertTrue(true)
    }
}
